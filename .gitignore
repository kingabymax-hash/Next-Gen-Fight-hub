# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env — never commit secrets
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# os
.DS_Store

# raw / source media masters (large; keep out of the repo). Web-optimised assets
# the site ships live in public/ and ARE tracked.
/media-masters
*.mov
*.MOV
